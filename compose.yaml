services:
  postgres:
    image: postgres:18
    container_name: bhupragati-db

    env_file:
      - .env.docker

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: